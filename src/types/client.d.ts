export type SheetProps = {
    title: string,
    headers: Array<string>,
    entries: Array<Array<string>>,
    key: string,
}

export type RGBColor = {
    red: number,
    green: number,
    blue: number,
}
